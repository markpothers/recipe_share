Chef#account_activation

<%= @greeting %>

    Thanks for joining Recipe-Share.
    Here's the activation link to complete setup of your account.

<%= "#{Rails.application.credentials.server[Rails.env.to_sym]}/activate?token=#{@chef.activation_digest}&email=#{@chef.e_mail.split("@").join("%40")}" %>

    You're just a click away from countless recipe ideas to enjoy.
    Thanks so much for signing up.
    Recipe-Share Admin"